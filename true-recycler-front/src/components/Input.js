import React from "react";

import "./Input.css";

const Input = () => {
  return (
    <label>
      Search by name
      <input type="text" className="input" />
    </label>
  );
};

export default Input;
